# Resolve only releases that have been public for at least 48 hours.
min-release-age=2

# A dependency lifecycle script must be reviewed and pinned in allowScripts.
strict-allow-scripts=true
allow-scripts-pin=true
dangerously-allow-all-scripts=false

# package-lock.json is the reproducible install contract.
package-lock=true
