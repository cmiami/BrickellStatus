[package]
name = "brickellstatus-model"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Transport-neutral domain models for the BrickellStatus signal console"
# Internal to this application: never published, and the path dependencies
# between these crates could not be published anyway.
publish = false

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"
